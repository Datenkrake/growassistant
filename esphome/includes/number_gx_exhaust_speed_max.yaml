platform: template
name: "GX Exhaust Speed Max [${area_id}_${device_id}${suffix}]"
id: ${area_id}_${device_id}${suffix}_gx_exhaust_speed_max
state_topic: "${area_id}/gx_exhaust_speed_max/state/${device_id}"
command_topic: "${area_id}/gx_exhaust_speed_max/state/${device_id}/set"
optimistic: True
initial_value: 100.0
min_value: 0.0
max_value: 100.0
step: 1.0
mode: box 
unit_of_measurement: "%"
retain: True
restore_value: True
